version: 2.1
jobs:
  build:
    docker:
      - image: node:13.8.0
    steps:
      - checkout
      - run:
          name: "run build commands"
          command: |
          "npm i"
          "npm run lint"
  test:
    docker:
      - image: node:13.8.0
    steps:
      - checkout
      - run:
          name: "run test commands"
          command: |
          "npm i"
          "npm run test"
  analyze:
    docker:
      - image: node:13.8.0
    steps:
      - checkout
      - run:
          name: "run analyze commands"
          command: "npm audit"
workflows:
  build-workflow:
    jobs:
      - build
      - test:
          requires:
            - build
      - analyze:
          requires: 
            - test
